# Project Profile — Slim Variant

> Auto-generated by Dark Factory onboard-agent. This template defines the structure and extraction rules for `dark-factory/project-profile-slim.md`.
> Target size: ~30 lines / ~500 tokens. For genuinely complex projects, exceeding the target is acceptable.

---

## Output Structure

When onboard-agent writes `dark-factory/project-profile-slim.md`, it MUST produce the following sections in this order:

### Section 1: Header Disclaimer (required — always include)

Exact text:

```
> Slim profile — generated from project-profile.md. For full context read project-profile.md.
```

Extraction rule: This line is fixed — copy it verbatim. It is not derived from the full profile.

---

### Section 2: Tech Stack (required — always include)

```markdown
## Tech Stack
| Layer | Technology |
|-------|-----------|
{rows from full profile Tech Stack table — verbatim}
```

Extraction rule: Copy the full Tech Stack table rows verbatim from the `## Tech Stack` section of `project-profile.md`. Include the table header row. Include all rows. Do not add or remove rows.

---

### Section 3: Critical Conventions (required — always include)

```markdown
## Critical Conventions
- {convention 1} — sourced from Architecture / Patterns to Follow or Structural Notes
- {convention 2}
- ... (3–5 total)
```

Extraction rule: Extract 3–5 bullet points from the **Architecture / Patterns to Follow** subsection and/or the **Structural Notes** section of the full profile. Choose the bullets that represent the most important conventions an agent must not violate when implementing any change. Rephrase for conciseness if needed, but preserve meaning exactly.

---

### Section 4: Entry Points (conditional — omit if full profile has no Entry Point Traces)

```markdown
## Entry Points
- {entry point name 1}
- {entry point name 2}
- {entry point name 3 (if applicable)}
```

Extraction rule: From the full profile's **Entry Point Traces** section (if present), list the top 2–3 entry point names only. Omit call chain prose, file paths, and descriptions. If the full profile has no Entry Point Traces section, omit this section entirely — do not write an empty heading.

---

### Section 5: Common Gotchas (conditional — omit if full profile has no Common Gotchas)

```markdown
## Common Gotchas
{verbatim copy of the Common Gotchas section from the full profile}
```

Extraction rule: Copy the **Common Gotchas** section verbatim from the full profile. If the full profile has no Common Gotchas section (e.g., greenfield project), omit this section entirely — do not write an empty heading.

---

## Derivation Rules Summary

| Slim Section | Source Section in Full Profile | How |
|---|---|---|
| Header disclaimer | N/A | Fixed text — always verbatim |
| Tech Stack | `## Tech Stack` | Table rows verbatim |
| Critical Conventions | `### Patterns to Follow` and/or `## Structural Notes` | 3–5 selected bullets |
| Entry Points | Entry Point Traces (from code-map or profile) | Top 2–3 names only |
| Common Gotchas | `## Common Gotchas` | Verbatim copy |

## Notes

- The slim file is a **derived, non-authoritative** artifact. Any consumer that needs a field not present in the slim file must read the full `project-profile.md`.
- The slim file is committed to git alongside the full profile. It is a permanent artifact, not an ephemeral cache.
- No developer confirmation is required for the slim file — the full profile confirmation covers both.
- **Org Context is not included in the slim profile.** The Org Context section from the full profile is intentionally excluded here — it is consumed only by spec-agent during spec authoring, not by architect-agents during review. If you need org context, read the full `project-profile.md`.
